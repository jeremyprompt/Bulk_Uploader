export const metadata = {
  title: 'Bulk Channel Creator',
  description: 'Bulk create channels in Prompt.io',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
} 